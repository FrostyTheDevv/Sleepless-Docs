{
  "name": "sleepless-commands",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "dev":   "live-server --entry-file=index.html --port=3000",
    "admin": "live-server --entry-file=admin.html --port=3001",
    "serve": "serve -s . -l 5000",
    "start": "npm run serve"
  },
  "devDependencies": {
    "live-server": "^1.2.1",
    "serve":       "^14.0.1"
  }
}